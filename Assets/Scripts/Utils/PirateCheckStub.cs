using UnityEngine;

namespace Utilities
{
    /// <summary>
    /// ЗАГЛУШКА ДЛЯ БУДУЩЕЙ САТИРИЧЕСКОЙ ПРОВЕРКИ ЛИЦЕНЗИИ.
    /// Сейчас скрипт не делает ничего, кроме вывода сообщения в консоль.
    /// 
    /// ИДЕЯ:
    /// При запуске неофициальной (пиратской) версии игры, вместо главного меню
    /// игрок должен пройти мини-квест "Служебная проверка №401":
    /// - Найти среди вороха одинаковых бумаг единственную "лицензию".
    /// - Получить забавное сообщение от Папочки по красному телефону.
    /// - В конце увидеть призыв поддержать разработчиков.
    /// 
    /// ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ (план):
    /// 1. Повесить этот скрипт на любой объект в MainMenuScene.
    /// 2. В методе Start() проверять наличие Steamworks и флаг официальной версии.
    /// 3. Если версия неофициальная, показывать Canvas с мини-игрой.
    /// 4. Для мини-игры создать префады "бумажек" с коллайдерами.
    /// 5. Правильная бумажка может иметь скрытый идентификатор (например, имя GameObject).
    /// 6. При клике на правильную бумажку загружать главное меню.
    /// 7. Все остальные бумажки просто уничтожаются (сминаются в урну).
    /// 8. Добавить таймер и красный телефон для шутки.
    /// </summary>
    public class PirateCheckStub : MonoBehaviour
    {
        [Header("Настройки проверки")]
        [Tooltip("Включена ли проверка? Пока выключена, чтобы не мешать разработке.")]
        [SerializeField] private bool enableCheck = false;

        [Header("Префабы для будущей мини-игры")]
        [Tooltip("Префаб обычной бумажки.")]
        [SerializeField] private GameObject paperPrefab;
        [Tooltip("Префаб правильной бумажки (лицензии).")]
        [SerializeField] private GameObject licensePaperPrefab;
        [Tooltip("Префаб красного телефона.")]
        [SerializeField] private GameObject redPhonePrefab;

        [Header("UI Canvas")]
        [Tooltip("Канвас для мини-игры (будет создан динамически).")]
        [SerializeField] private Canvas pirateCanvas;

        private void Start()
        {
            if (!enableCheck)
            {
                Debug.Log("[PirateCheck] Проверка отключена. Когда-нибудь здесь будет сатирическая DRM.");
                return;
            }

            // Заглушка проверки: всегда считаем, что версия официальная
            if (IsOfficialVersion())
            {
                Debug.Log("[PirateCheck] Официальная версия. Загружаем главное меню.");
                // Здесь будет переход в главное меню
            }
            else
            {
                Debug.Log("[PirateCheck] Пиратская версия! Запускаем служебную проверку №401.");
                // Здесь будет запуск мини-игры
                StartPirateCheck();
            }
        }

        /// <summary>
        /// Проверяет, запущена ли игра через Steam и имеет ли AppID.
        /// ПОКА ВСЕГДА ВОЗВРАЩАЕТ TRUE, ЧТОБЫ НЕ МЕШАТЬ РАЗРАБОТКЕ.
        /// </summary>
        private bool IsOfficialVersion()
        {
            // TODO: Реализовать настоящую проверку через SteamManager
            // Пример:
            // #if !DISABLESTEAMWORKS
            // return SteamManager.Initialized;
            // #else
            // return false;
            // #endif
            
            return true; // Заглушка
        }

        /// <summary>
        /// Запускает мини-игру "Служебная проверка".
        /// </summary>
        private void StartPirateCheck()
        {
            // TODO: Создать UI с бумажками, урной, телефоном.
            // Примерная логика:
            // - Создать Canvas (если не назначен).
            // - Разбросать N обычных бумажек и 1 лицензию.
            // - При клике на обычную бумажку уничтожать её.
            // - При клике на лицензию загружать MainMenuScene.
            // - При клике на телефон показывать сообщение от Папочки.
            Debug.Log("[PirateCheck] Заглушка мини-игры. Здесь будет интерактивный разбор бумаг.");
        }

        // Методы-заглушки для будущих событий UI
        public void OnPaperClicked(GameObject paper)
        {
            // TODO: Уничтожить бумажку, проиграть звук смятия
        }

        public void OnLicenseClicked()
        {
            // TODO: Загрузить главное меню, показать сообщение
        }

        public void OnPhoneClicked()
        {
            // TODO: Показать диалог Папочки
        }
    }
}
